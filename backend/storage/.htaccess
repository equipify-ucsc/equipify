# Uploaded files (credential documents, portfolio images, equipment photos) are
# never served straight from disk. They are read through a controller that
# checks permission first, so every direct request here is refused.
Require all denied
